Toggle switch (support on/off, chamber light, preferences, send-print options).

```jsx
<Switch checked={lamp} onChange={setLamp} />
```

On: primary track + on-primary 16px knob. Off: transparent track, outline border, 12px outline knob.
