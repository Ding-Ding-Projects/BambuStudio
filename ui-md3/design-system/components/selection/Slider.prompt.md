Range slider — fan speeds, Preview layer (vertical) and move timeline (horizontal).

```jsx
<Slider min={0} max={100} value={fan} onChange={setFan} />
<Slider vertical min={1} max={180} value={layer} onChange={setLayer} />
```
