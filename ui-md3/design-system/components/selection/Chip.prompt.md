Selectable filter chip (G-code color schemes, export filters, regex flags).

```jsx
<Chip label="Line type" selected onClick={...} />
<Chip label="Travel" icon="route" selected={opts.travel} />
<Chip label="Bambu Lab" tonal selected />
```

Unselected: transparent + 1px outline border + on-surface-variant text.
