Borderless icon button; hover = sc-high wash. Used everywhere: toolbars, row actions, dialog close.

```jsx
<IconButton icon="close" title="Close" />
<IconButton icon="close" shape="square" danger />  {/* window close */}
<IconButton icon="edit" size={32} filled />
```
