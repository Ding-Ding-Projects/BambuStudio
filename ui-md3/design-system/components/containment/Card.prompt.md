The standard panel/card (Device panels, calibration cards, quick actions).

```jsx
<Card interactive radius={20}>…</Card>
```

Cards layer by surface-container step, not shadow; hover = primary border.
