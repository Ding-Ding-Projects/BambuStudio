Confirmation toast — past-tense copy.

```jsx
<Snackbar icon="history" message="Restored project to #a1f9c02" actionLabel="Undo" onDismiss={...} />
```

Stack in a bottom-centered column-reverse flex, width min(560px, 92vw).
