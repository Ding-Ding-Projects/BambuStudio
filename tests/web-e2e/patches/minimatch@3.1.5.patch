diff --git a/minimatch.js b/minimatch.js
index 2e2c87a..691694f 100644
--- a/minimatch.js
+++ b/minimatch.js
@@ -10,1 +10,6 @@
-var expand = require('brace-expansion')
+// brace-expansion 5 exports a named `expand` function, while the legacy
+// versions accepted by minimatch 3 exported the function directly. Accept
+// both shapes so the security-patched 5.0.8 override remains API-compatible.
+var braceExpansion = require('brace-expansion')
+var expand = typeof braceExpansion === 'function'
+  ? braceExpansion : (braceExpansion.expand || braceExpansion.default)
